# -*- snippet -*-
# name : test template for Nakamap
# --
use HTTP::Request::Common;
use Test::More;

use Nakamap::Models;
use Nakamap::Test;

init_table_all();

my $conf = M('conf');

${1:test_api sub {
    my ($cb) = @_;

    my $base_uri = URI->new(  );
};}
$0
done_testing;
