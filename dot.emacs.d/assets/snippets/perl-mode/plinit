# -*- mode: snippet -*-
# name: init pl file
# --
#!/usr/bin/env perl

use strict;
use warnings;
use feature 'say';

${1:use App::Options(
    option => {
        $0
    },
);
my %opts = %App::options;}
